<IfModule mod_rewrite.c>
    RewriteEngine On
    # Redirect all requests to the public/ subdirectory
    RewriteRule ^$ public/ [L]
    RewriteRule ^((?!public/).*)$ public/$1 [L]
</IfModule>

# Prevent direct index browsing
Options -Indexes
